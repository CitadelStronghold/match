name: 'match'
inputs:
  match:
    type: string
    required: true
runs:
  using: 'composite'
  steps:
    - name: Match
      shell: bash
      run: |
        ls result.log || exit -1
        cat result.log

        result='$(cat result.log)'

        ([[ "$result" =~ (${{ inputs.match }}) ]] && echo "Matched: ${{ inputs.match }}") || (echo "!Matched: ${{ inputs.match }}" && exit -1)